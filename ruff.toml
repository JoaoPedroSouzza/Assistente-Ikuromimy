target-version = "py310"
extend-exclude = [".venv-testes", "reports", "reports-ampliados", "reports-pipeline"]

[lint]
# Gate inicial para erros de execução; estilo do legado será tratado separadamente.
select = ["E9", "F63", "F7", "F82"]
