[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --strict-config
qt_api = pyside6
markers =
    windows: teste que exige Windows real
    integration: integração entre módulos reais, com efeitos externos simulados
